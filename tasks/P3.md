# Task P3: 链接与常驻 — OrcaTab.app、install.sh、复制链接、pm2 文档

（P2 已验收合并。CLI 回退路径 `bun src/focus.ts <uri>` 在 P2 已实现：无库时直接扫 jsonl 取 cwd，只读打开库；本工单不要改 `src/focus.ts` 的解析逻辑。进程入口是 `src/main.ts`；`src/` 下禁止顶层 `await`。）

## Requirements
1. `scheme/handler.applescript`：
   ```applescript
   on open location theURL
     set q to quoted form of theURL
     set viaServer to "/usr/bin/curl -sfG --max-time 5 --data-urlencode uri=" & q & " http://127.0.0.1:47831/focus"
     set fallback to "/opt/homebrew/bin/bun " & quoted form of "/Users/bb00/workspace/orcatab/src/focus.ts" & " " & q
     do shell script viaServer & " || " & fallback
   end open location
   ```
2. `scheme/install.sh`（幂等）：`osacompile -o "$HOME/Applications/OrcaTab.app" scheme/handler.applescript`；PlistBuddy 设 `CFBundleIdentifier=dev.local.orcatab`，无 `CFBundleURLTypes` 时添加 `[{CFBundleURLName: OrcaTab, CFBundleURLSchemes: [orcatab]}]`；`lsregister -f "$APP"`；最后打印验证命令 `open "orcatab://claude/<sid>"`。**agent 只写脚本，不执行**（由协调者执行）。
3. `scheme/uninstall.sh`：`lsregister -u` + 删除 app。
4. README：安装、pm2 托管（`pm2 start ecosystem.config.cjs && pm2 save`）、`orcatab://` 用法、环境变量表。
5. 页面「复制链接」已在 P1；此处补「复制命令」（`claude --resume <sid>`）。
6. 提交：`feat: P3 orcatab:// scheme handler, install scripts, docs`。

## Acceptance
- `bash -n scheme/install.sh scheme/uninstall.sh` 通过；`osacompile -o "$(mktemp -d)/OrcaTab.app" scheme/handler.applescript` 编译通过（临时目录，不注册）。
- `bun test` 全绿；`bun x tsc --noEmit` exit 0。
- README 里的环境变量表与 `docs/PLAN.md` 一致（ORCATAB_PORT / ORCATAB_CLAUDE_DIR / ORCATAB_DATA_DIR / ORCATAB_ORCA_BIN）。

## Deliverable
`tasks/REPORT-P3.md` ≤ 10 行。
