# Task P2: 增量与离线 — fs.watch、ETag、离线 resume 实测

（P1 完成并经协调者验证后发出；届时可能微调。）

## Requirements
1. `src/indexer.ts`：`fs.watch(<ORCATAB_CLAUDE_DIR>/projects, {recursive:true})`，事件 500 ms 防抖后跑一次增量；保留定时兜底（改为 60 s）。watch 失败（EMFILE 等）→ 打日志并退回 10 s 定时。
2. `src/db.ts` / `src/server.ts`：维护 `dataVersion`（每次索引变更或 live 状态变化 +1）；`/api/projects`、`/api/sessions` 返回 `ETag: "<version>"`，请求带匹配 `If-None-Match` → 304 空体。页面轮询带 `If-None-Match`。
3. 页面：新增「最近更新」提示（索引时间）；离线会话按钮「恢复」的结果态文案；`manual` 时展示命令与「复制命令」。
4. `focus.ts` 真实 `orca terminal create` 响应字段以 P1 报告为准修正；补一个 `--dry-run` 下打印将执行的 argv 列表（便于协调者核对）。
5. 测试：watch 防抖（用临时目录写文件触发）、ETag 304、dataVersion 递增；覆盖率维持 ≥ 80%。
6. 提交：`feat: P2 incremental watch, ETag polling, offline resume`。

## Acceptance
- `bun test` 全绿；`bun x tsc --noEmit` exit 0。
- 冒烟：起服务后向临时 fixture 追加一行 prompt，1 s 内 `/api/sessions` 反映新 `lastInputAt`。
- `curl -i /api/sessions` 两次，第二次带 `If-None-Match` 返回 304。

## Deliverable
`tasks/REPORT-P2.md` ≤ 12 行。约束同 P1（离线 resume 的真实执行由协调者做）。
